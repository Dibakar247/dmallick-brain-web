# Projects

One file per project. Updated by `index-workspace.py` and agents during sessions.

## Status values
- `active` — currently being worked on
- `paused` — on hold
- `archived` — done / deprecated

## Fields
Each file has YAML frontmatter: type, status, last_updated, language, workspace_path
